{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Modificar grupo {{ grupo.descripcion }}</h1>

    {#{{ form(formulario) }}#}
    {# Lo de arriba equivale a lo de abajo#}
    {{ form_start(formulario) }}
    {{ form_widget(formulario) }}

    <div class="form-group">
        <label class="col-sm-2"></label>
        <div class="col-sm-10">
            <button type="submit" id="grupo_enviar" name="grupo[enviar]" class="btn btn-success btn">
                <i class="fa fa-check"></i> Guardar cambios
            </button>
        </div>
    </div>
    {{ form_end(formulario) }}


    {#
    {{ form_start(formulario) }}
    {{ form_row(formulario.aula) }}
    <hr/>
    {{ form_row(formulario.descripcion) }}
    {{ form_end(formulario) }}
    #}
{% endblock %}

